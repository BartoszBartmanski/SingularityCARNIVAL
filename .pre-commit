#!/usr/bin/env bash

for dir in */; do 
    cd ${dir}
    # If the sif file doesn't exist, build the image
    if [ ! -f image.sif ]; then 
        sudo singularity build image.sif image.def
    fi
    # Sign the build image
    singularity sign image.sif
    cd ../
done
